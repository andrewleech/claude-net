# strace captures — asyncio+TLS idle-poll bug

Packaged claude-net plugin, idle on the live hub (wss://), linux-x64 mcp variant.
Generated by `../trace_idle.sh` / `../repro_idle_cpu.py`. See `../README.md` for
the full root-cause writeup.

- `strace_syscalls_period1.txt`  — histogram, 1ms period (baseline/bug):
  ~45k poll() in 30s (~1500/s), idle CPU ~2.45%.
- `strace_syscalls_period50.txt` — histogram, 50ms period (mcp-variant fix):
  ~1.2k poll() in 30s, idle CPU ~0.07%.
- `strace_timeline_period1_window.txt` — annotated steady-state extract showing
  the poll([fd=3],1,0) + poll([fd=0],1,1) two-call cycle.
- `strace_timeline_period1_full.txt`   — the full ~8s timeline it came from.

`newfstatat` ~10/s with ENOENT is the transcript rename-watch stat'ing a path
that doesn't exist in these repro cwds; unrelated to the poll bug.
